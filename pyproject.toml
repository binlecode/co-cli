[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "co-cli"
version = "0.3.3"
description = "Co — personal AI assistant CLI with sandboxed shell, Google Drive/Gmail/Calendar, Slack, Obsidian, and OTel tracing"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasette>=0.65.2",
    "docker>=7.1.0",
    "google-api-python-client>=2.189.0",
    "google-auth-httplib2>=0.3.0",
    "google-auth-oauthlib>=1.2.4",
    "google-genai>=1.61.0",
    "opentelemetry-sdk>=1.39.1",
    "prompt-toolkit>=3.0.52",
    "pydantic-ai>=1.52.0",
    "rich>=14.3.2",
    "slack-sdk>=3.39.0",
    "typer>=0.21.1",
    "datasette-pretty-json>=0.3",
    "httpx>=0.28.1",
    "html2text>=2025.4.15",
]

[project.scripts]
co = "co_cli.main:app"

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "pytest-cov>=7.0.0",
]
