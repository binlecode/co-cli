#!/usr/bin/env bash
# Hooks are now version-controlled in .githooks/.
# Activate them once after cloning:
#
#   git config core.hooksPath .githooks
#
# See CLAUDE.md setup section for details.
echo "Hooks are version-controlled in .githooks/. Run: git config core.hooksPath .githooks"
