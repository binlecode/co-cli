# Split `_history.py` into `_compaction.py` + `_prompt_text.py`

## Context

`co_cli/context/_history.py` is 1256 lines and currently hosts three orthogonal responsibilities:

1. **Compaction subsystem** (lines 1-1090) — history processors (dedup, truncate, enforce_batch_budget, summarize_history_window), the shared planner (plan_compaction_boundaries, markers, group_by_turn), the hygiene and overflow-recovery entry points, and the semantic markers / prefixes.
2. **Dynamic-instruction text** (lines 1099-1261) — `_recall_prompt_text` (per-turn personality + knowledge recall) and `_safety_prompt_text` (per-turn doom-loop / shell-error warnings). These are registered via `agent.instructions()` and never appear in `message_history`.

These two subsystems do not share state and only overlap incidentally (both happen to scan messages for the last user turn). Splitting clarifies ownership, shrinks the surface of the compaction module to just compaction, and lets specs point at a file that represents exactly what it claims to.

This is the deferred item from the 2026-04-23 compaction-cleanup pass (plan: `docs/exec-plans/completed/2026-04-23-143810-compaction-todo-snapshot.md` after ship). It is a pure refactor — **no behaviour changes**.

## Files Touched

| Action | File | Nature |
|--------|------|--------|
| Create | `co_cli/context/_compaction.py` | New — receives all compaction symbols |
| Create | `co_cli/context/_prompt_text.py` | New — receives instruction-text symbols |
| Delete | `co_cli/context/_history.py` | Deleted after migration (no re-export shim) |
| Modify | `co_cli/agent/_core.py:15` | Update processor imports |
| Modify | `co_cli/agent/_instructions.py:10,17` | Update prompt-text imports |
| Modify | `co_cli/context/orchestrate.py:65,526` | Update compaction imports |
| Modify | `co_cli/commands/_commands.py:335` | Update `_build_compaction_marker` / `_build_todo_snapshot` import + hoist to top-level |
| Modify | `co_cli/prompts/_assembly.py:15` | Update `COMPACTABLE_KEEP_RECENT` import |
| Modify | `evals/eval_compaction_quality.py:67,395,733,1945` | Update 4 import sites |
| Modify | `evals/eval_memory_extraction_flow.py:341` | Update `_recall_prompt_text` import |
| Modify | `evals/eval_memory_recall.py:45` | Update `_recall_prompt_text` import |
| Modify | `tests/test_context_compaction.py:19` | Update imports |
| Modify | `tests/test_prompt_assembly.py:4` | Update `COMPACTABLE_KEEP_RECENT` import |
| Modify | `tests/test_dedup_tool_results.py:23` | Update imports |
| Modify | `tests/test_tool_result_markers.py:19` | Update imports |
| Modify | `tests/test_history.py:26` | Update imports; consider renaming file to `test_compaction.py` + `test_prompt_text.py` (see below) |
| Modify | `tests/test_transcript.py:234,268` | Update deferred imports inside two tests |
| Modify | `docs/specs/core-loop.md:404` | Point row at `_compaction.py` + `_prompt_text.py` |
| Modify | `docs/specs/compaction.md:632,660` | Replace `_history.py` references |
| Modify | `docs/specs/prompt-assembly.md:141` | Split the row between the two new modules |

**Total: 2 new files, 1 deletion, ~18 files edited. ~60 import statements updated, 3 specs updated.**

## Symbol Inventory

Every top-level symbol in `_history.py` lands in exactly one of the two new modules. No re-exports.

### → `_compaction.py` (~1090 lines)

**Types & planner**
- `TurnGroup` (dataclass), `_CompactionBoundaries` (type alias)
- `_make_turn_group`, `group_by_turn`, `groups_to_messages`, `find_first_run_end`
- `_anchor_tail_to_last_user`, `plan_compaction_boundaries`
- `_find_last_turn_start` — **lives here** (primary users are the processors)

**Constants**
- `COMPACTABLE_KEEP_RECENT`, `_CLEARED_PLACEHOLDER`
- `_CONTEXT_MAX_CHARS`, `_SUMMARY_MARKER_PREFIX`, `_TODO_SNAPSHOT_PREFIX`
- `_MIN_RETAINED_TURN_GROUPS` (if still present)

**Markers**
- `_static_marker`, `_summary_marker`, `_build_compaction_marker`
- `_build_todo_snapshot`

**Processors**
- `dedup_tool_results` + `_build_latest_id_by_key`
- `truncate_tool_results` + `_build_keep_ids`, `_build_call_id_to_args`, `_build_cleared_part`, `_iter_tool_returns_reversed`, `_rewrite_tool_returns`
- `enforce_batch_budget` + `_tool_return_part_size`, `_collect_batch_parts`, `_apply_batch_replacements`

**Context gatherers**
- `_gather_file_paths`, `_active_todos`, `_gather_session_todos`
- `_gather_prior_summaries`, `_gather_compaction_context`

**Summarization + compaction core**
- `_circuit_breaker_should_skip`, `_summarize_dropped_messages`
- `_preserve_search_tool_breadcrumbs`, `_apply_compaction`
- `_effective_token_count`
- `summarize_history_window`, `maybe_run_pre_turn_hygiene`
- `recover_overflow_history`, `emergency_recover_overflow_history`, `emergency_compact`

### → `_prompt_text.py` (~165 lines)

**Last-user helpers**
- `_get_last_user_message`, `_count_user_turns`

**Dynamic instruction text**
- `_recall_prompt_text`

**Safety signals**
- `_count_consecutive_same_calls`, `_is_shell_error_return`, `_count_consecutive_shell_errors`
- `_safety_prompt_text`

### Cross-module import

`_prompt_text.py` has **no imports from `_compaction.py`**. The last-user helpers live independently — `_get_last_user_message` does its own part-level scan (which differs from `_find_last_turn_start` by filtering for string content). Avoids any circular-import concern.

## Migration Steps

1. **Create `_compaction.py`** with the compaction-section symbols + the existing module docstring trimmed to compaction-only.
2. **Create `_prompt_text.py`** with the prompt-text symbols + a docstring stating "Dynamic instruction implementations called via `agent.instructions()` wrappers in `agent/_instructions.py`."
3. **Update every importer** (18 files listed above). Prefer fewer import lines: the `commands/_commands.py:335` deferred import of `_build_compaction_marker`, `_build_todo_snapshot` should hoist to a top-level `from co_cli.context._compaction import _build_compaction_marker, _build_todo_snapshot` once the cycle risk is gone — see "Follow-up nit" below.
4. **Delete `_history.py`**. Per CLAUDE.md ("avoid backwards-compat hacks"), no `from _compaction import *` re-export shim.
5. **Verify zero stale references**: `rg "_history" co_cli tests evals docs` returns only unrelated matches (e.g. `message_history` — the variable name, `chat_history` — unrelated). A focused check: `rg "co_cli\.context\._history|from co_cli\.context\._history|context\._history" co_cli tests evals docs` must be empty.
6. **Update specs** — see below.

## Spec Updates

### `docs/specs/core-loop.md:404`

Current:
```
| `co_cli/context/_history.py` | three registered history processors (...); `maybe_run_pre_turn_hygiene` (...); `_recall_prompt_text` and `_safety_prompt_text` (...) |
```

Replace with two rows:
```
| `co_cli/context/_compaction.py` | three registered history processors (tool-output trim, batch-budget cap, sliding-window compaction); `maybe_run_pre_turn_hygiene` (M0 pre-turn hygiene); `plan_compaction_boundaries`; overflow recovery entry points |
| `co_cli/context/_prompt_text.py` | `_recall_prompt_text` and `_safety_prompt_text` (dynamic instruction implementations, called via `agent.instructions()` wrappers in `_instructions.py`) |
```

### `docs/specs/compaction.md:632,660`

Line 632: `"Non-configurable module constants in co_cli/context/_history.py:"` → `"… in co_cli/context/_compaction.py:"`.

Line 660 (Files table): change `co_cli/context/_history.py` row to `co_cli/context/_compaction.py` and drop any mention of `_recall_prompt_text` / `_safety_prompt_text` (they belong in prompt-assembly.md, not compaction.md).

Line 672 (Tests table): if `tests/test_history.py` is split into `test_compaction.py` + `test_prompt_text.py`, update accordingly. Otherwise leave.

### `docs/specs/prompt-assembly.md:141`

Current:
```
| `co_cli/context/_history.py` | three registered history processors (...); `_recall_prompt_text` and `_safety_prompt_text` (...); compaction trigger |
```

Split into two rows (or better: the compaction row belongs in `compaction.md`, keep only the prompt-text row here):
```
| `co_cli/context/_prompt_text.py` | `_recall_prompt_text` and `_safety_prompt_text` — dynamic instruction implementations called via `agent.instructions()` wrappers |
```

## Optional: split `tests/test_history.py`

`tests/test_history.py` (~1000 lines per memory from recent work) mixes compaction planner tests, `/compact` dispatch tests, and recall / safety-prompt tests. It is the natural counterpart to split:

- `tests/test_compaction.py` — planner, markers, hygiene, overflow, processor tests.
- `tests/test_prompt_text.py` — recall prompt text, safety prompt text, doom-loop / shell-error counters.

**Decision point.** This is mechanical but not coupled to the src split. Leaving it as `test_history.py` is valid for a focused diff; splitting is cleaner and should be a single follow-up commit. Recommend splitting **in the same PR** to keep filename-to-subject pairing consistent — it is cheaper to split tests once than to untangle them later.

## Follow-up Nit Bundled In

**`_cmd_compact` deferred imports.** `co_cli/commands/_commands.py:329-340` has three deferred-import blocks:

```python
from pydantic_ai.exceptions import ModelAPIError, ModelHTTPError
from pydantic_ai.messages import ModelResponse
from pydantic_ai.messages import TextPart as _TextPart

from co_cli.context._history import _build_compaction_marker, _build_todo_snapshot
from co_cli.context.summarization import (
    estimate_message_tokens,
    resolve_compaction_budget,
    summarize_messages,
)
```

After the split:

- The `_build_compaction_marker` / `_build_todo_snapshot` import points at `_compaction.py` — hoist to top-level.
- The other two (pydantic_ai and summarization) are deferred because `_commands.py` is imported at startup and we don't want to pay for heavy compaction-machinery imports on every process spawn. **Leave them deferred** unless `python -c "import co_cli.commands._commands"` shows measurable regression from hoisting (measure before/after).

Verification for the hoist: `python -m pyperf timeit "import co_cli.commands._commands"` ring around the change. If import cost rises > 30ms, revert the hoist.

## Verification

Done means all of the following pass:

1. **Grep is clean.** `rg "co_cli\.context\._history|from co_cli\.context\._history|context\._history" co_cli tests evals docs` returns zero matches.
2. **Unit tests.** `scripts/quality-gate.sh lint && uv run pytest 2>&1 | tee .pytest-logs/$(date +%Y%m%d-%H%M%S)-history-split-full.log` — all tests pass.
3. **Evals still import cleanly.** `uv run python -c "import evals.eval_compaction_quality; import evals.eval_memory_extraction_flow; import evals.eval_memory_recall"` — no ImportError.
4. **REPL smoke.** `uv run co chat` starts; run a short conversation; invoke `/compact`; force a long reply to trigger auto-compaction; confirm the prior-summary marker is detected by `_gather_prior_summaries` (grep `.co-cli/logs/` for "Prior summary").
5. **Spec greps.** After spec updates, `rg "_history\.py" docs/specs/` returns zero matches.
6. **No stale symbols in imports.** Each edited importer now imports from exactly one of `_compaction` or `_prompt_text`, not both — unless a file genuinely touches both concerns (only `tests/test_history.py` does, if left unsplit).

## Risks & Mitigations

| Risk | Mitigation |
|------|------------|
| Circular import between `_compaction.py` and `_prompt_text.py` | Verified above: no cross-imports required. |
| Accidentally orphaning a symbol during migration | Run the grep in step 1 after every file edit — treat a non-empty result as a blocker. |
| Test file rename breaking CI `--durations` or test discovery | Pytest autodiscovers by `test_*.py` / `*_test.py` — rename is safe. If CI has pinned test paths, grep CI configs first. |
| Downstream consumers (if any) pinning to `_history.py` | None found — all importers are inside this repo. Evals are also in-tree. |
| Eval file imports breaking during long-running eval runs | Evals are one-shot scripts; a clean `python -c "import ..."` before running confirms. |

## Critical Files

- `co_cli/context/_history.py` — source of the split
- `co_cli/agent/_core.py:15` — processor import (will fail first if split is wrong)
- `co_cli/agent/_instructions.py:10,17` — instruction registrations
- `co_cli/context/orchestrate.py:65,526` — hygiene + overflow recovery imports
- `co_cli/commands/_commands.py:329-340` — `/compact` handler; also the candidate for the deferred-import hoist
- `co_cli/prompts/_assembly.py:15` — the one cross-package constant import
- `docs/specs/core-loop.md:404`, `docs/specs/compaction.md:632,660`, `docs/specs/prompt-assembly.md:141` — spec doc rows that call out `_history.py` by path
