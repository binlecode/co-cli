# Skill Port — `documents` (PDF extraction over `shell_exec`)

Task type: code + skill content

> **Built on the folder-skill model** — every skill is a `<name>/SKILL.md` folder that can
> carry bundled assets. Shipped in **v0.8.332**
> (`docs/exec-plans/completed/2026-06-09-171515-skill-folder-model.md`). This plan adds
> `documents/SKILL.md` + `documents/scripts/extract_pdf.py` on top of it.

## Execution order (document-stack plans)

This is the **foundation plan** of a four-plan document stack; ship it **first**. The
dependency edges (verified against each plan's prerequisites):

1. **`skill-documents`** (this plan) — creates `extract_pdf.py`, the `co-extract-pdf` entry
   point, and `documents/SKILL.md`. **No prerequisites.** Everything else extends these.
2. **`skill-office`** (`2026-06-10-114024-skill-office.md`) — **after** this plan. Reciprocal
   routing: `office`'s body routes `.pdf` → `documents`, so `documents` must exist first; once
   `office` lands, the Office-format branch here can hard-route to it (until then this plan
   degrades honestly — see High-Level Design layer 3). Independent of the vision/scanned track.
3. **`vision-input`** (`2026-05-28-150239-vision-input.md`) — provides `image_view`.
   Independent of the document plans; can ship anytime, but is a **hard prerequisite of #4**.
4. **`scanned-pdf-vision`** (`2026-06-10-144745-scanned-pdf-vision.md`) — tier 2, ships
   **last**. Explicitly gated on **both** this plan (it adds `--render` to `extract_pdf.py` and
   keys off Constraint 7's scanned signal) **and** `vision-input` (`image_view`). It also edits
   `documents/SKILL.md`.

**Serialization note:** plans #2 and #4 both edit `documents/SKILL.md` (different branches of
the same decision tree — Office routing vs the scanned branch). Sequence them
(`documents` → `office` → `scanned-pdf-vision`) rather than running them concurrently, to keep
the SKILL.md edits conflict-free. `vision-input` can proceed on a parallel track and only needs
to be shipped before `scanned-pdf-vision` starts.

## Context

The most critical of co's three real functional tooling gaps vs hermes
(`docs/reference/RESEARCH-tools-gaps-co-vs-hermes.md` §2.1, §5): co cannot read a local
PDF — `file_read` rejects binary. This is squarely on-mission ("Personal Assistant CLI"):
PDFs are the most common local artifact a user hands an assistant. It is also the
cheapest of the three to close (a lean dep + a thin script + a skill — no new tool, no
model-multimodal plumbing). This plan was split out of the retired
`2026-05-27-133104-skill-porting-mission-converged.md`; the other two gaps (vision,
interactive terminal) have their own plans.

**Why a skill + script, not a tool.** The primary reason is that it adds **zero new
model-visible surface and zero `categories.py`/`toolset.py` churn** — it rides the existing
ALWAYS `shell_exec` + bundled-skill rails. The secondary benefit is process isolation:
extraction runs in a **subprocess** via `shell_exec`, so a memory spike or crash in pymupdf
on a large/malformed PDF cannot touch the agent heap (in-process `asyncio.to_thread` shares
it; a subprocess does not). A dedicated `document_extract` tool was rejected on both counts.

**Where the script lives — inside the skill folder, never `scripts/`.** `scripts/` at the
repo root is build-time-only dev tooling (quality-gate, audits, validators) and hatchling
ships **only `co_cli/`** in the wheel, so a bundled skill referencing `scripts/extract_pdf.py`
would find nothing on an installed system. With the folder-skill model (the prerequisite
plan), the helper ships **inside the skill's own folder** at
`co_cli/skills/documents/scripts/extract_pdf.py`. It is still reached **only** through
`shell_exec` (never imported into the agent process), so the subprocess-isolation boundary
is unchanged. `documents/` and `documents/scripts/` become packages (docstring-only
`__init__.py`) so the module path import-resolves.

**How the agent invokes it — a console entry point, not `uv run python -m`.** The agent
reaches the script through a `[project.scripts]` console entry point
`co-extract-pdf = "co_cli.skills.documents.scripts.extract_pdf:main"`, declared in
`pyproject.toml` exactly as `co` itself is (`pyproject.toml:29-30`). The skill body calls the
bare command `co-extract-pdf <path>`. **Why not `uv run python -m …`** — code-grounded in the
spawn path (verified 2026-06-10): `shell_exec` → `shell_backend.run_command` →
`asyncio.create_subprocess_exec` runs with a **sanitized allowlist env** (`shell_env.py:12-23`)
that propagates `PATH` but **not** `VIRTUAL_ENV` or `PYTHONPATH`, and the subprocess `cwd` is
the user's `workspace_dir` (`execute.py:78,85`), **not** co's repo. Under that env, `uv run`
resolves its environment from `cwd` — i.e. the *user's* project, which has no `co-cli`
dependency (and on an installed wheel, no project env at all) → `ModuleNotFoundError`. It only
works in the one narrow case of a developer sitting inside the co checkout — the very
install-location dependency the in-folder placement was chosen to escape. Plain `python -m …`
fails too: a `uv tool install` puts only entry-point scripts on `PATH`, not the tool venv's
`python`. A `[project.scripts]` entry point sidesteps all of this — the installer (uv sync /
uv tool install / pipx / pip) writes a wrapper into the same `bin/` as `co`, hard-coding the
correct interpreter; it depends on nothing but `PATH` containing that `bin/`, which is
guaranteed because the user launched `co` from there. Works identically in dev checkout and
installed wheel. The `__init__.py` files are still required (the `:main` module path must
import-resolve); `extract_pdf.py` exposes a `main()` function as the entry-point target.

### Code Accuracy Verification (source-read 2026-06-10, re-verified v0.8.336)

| Claim | Verified | Cite |
|---|---|---|
| `file_read` rejects binary (`UnicodeDecodeError`); no local PDF path | ✓ | `co_cli/tools/files/read.py:396,453` |
| `shell_exec(cmd, timeout=120, work_dir=None)`, ALWAYS, approval-gated, `work_dir` confined | ✓ | `co_cli/tools/shell/execute.py:17` |
| `file_search` (ALWAYS) does path-glob discovery (`content: str \| None = None`) — **replaces removed `file_find`** | ✓ | `co_cli/tools/files/read.py:478,481` |
| `web_fetch(url, format, timeout)`, ALWAYS | ✓ | `co_cli/tools/web/fetch.py` |
| No `python`/`uv run`/`co-extract-pdf` in `DEFAULT_SHELL_SAFE_COMMANDS` (first extraction prompts approval) | ✓ | `co_cli/config/shell.py:14` |
| `shell_exec` subprocess env is an **allowlist** (`shell_env.py:12-23`): `PATH` propagates, `VIRTUAL_ENV`/`PYTHONPATH` do **not**; `cwd` = user `workspace_dir`, not co's repo — so `uv run python -m` cannot resolve `co_cli`; a `[project.scripts]` console entry point can (resolves via `PATH`) | ✓ | `co_cli/tools/shell_env.py:12`, `co_cli/tools/shell/execute.py:78` |
| `print()`/`print(…, file=sys.stderr)` in `co_cli/` trips ruff **T20** — per-file-ignores exempt only `evals/`/`scripts/`/`tests/`/`strip_audit_log.py`, NOT `co_cli/skills/*/scripts/` — so the script must emit via `sys.stdout.write`/`sys.stderr.write` | ✓ | `pyproject.toml:59,77-82` |
| Bundled skills live in `co_cli/skills/<name>/SKILL.md` folders; name = `path.parent.name`, glob `*/SKILL.md` (shipped v0.8.332) | ✓ | `co_cli/skills/loader.py:89,141` |
| `scripts/` at repo root is build-time dev tooling, NOT in the hatchling wheel | ✓ | `pyproject.toml:1-3`; `scripts/` (quality-gate/audits/validators) |
| Bundled gate is `tests/test_flow_skill_bundled_library.py`; registration = add name to `_BUNDLED_NAMES` | ✓ | test file lines 10–17 |
| Skill tests are **flat** in `tests/` — there is **no** `tests/test_flow_skills/` dir | ✓ | `ls tests/` |
| Lint R1–R4 advisory; description ≤1024 chars; body warning at 8000; B1 = no TODO/FIXME/XXX in bundled | ✓ | `co_cli/skills/lint.py:23,24,89` |
| pyproject `[project].dependencies` is a flat list; `trafilatura`/`html2text` already present | ✓ | `pyproject.toml:4,14,15` |

**Corrections vs the retired plan:** (1) the Locate phase used the removed `file_find`
— this plan uses `file_search` (`content=None`); (2) the test path was
`tests/test_flow_skills/test_flow_skill_documents.py` — that dir does not exist; the test
is flat: `tests/test_flow_skill_documents.py`; (3) the helper script was placed in repo-root
`scripts/` — that dir is build-time-only and unshipped, so the script moves inside the skill
folder at `co_cli/skills/documents/scripts/extract_pdf.py` (the folder-skill model that makes
this shippable is now in code — shipped v0.8.332).

### Hermes parity reference (code-verified 2026-06-09)

Hermes ships `skills/productivity/ocr-and-documents/SKILL.md` (v2.3.0) with
`scripts/extract_pymupdf.py` (argparse: positional `document.pdf`, `--markdown` via
`pymupdf4llm`, `--pages 0-4` range, `--tables`, `--images`, `--metadata`) and
`scripts/extract_marker.py` (heavy OCR, marker-pdf ~3–5 GB). The skill encodes a decision
tree: URL → `web_extract` first; local text PDF → pymupdf; scanned/OCR → marker.
**co mirrors the shape, trimmed:** one `documents/scripts/extract_pdf.py` (pymupdf4llm only,
no marker-pdf), URL → `web_fetch`, local non-PDF → `file_read`; adopts hermes's argparse
convention (positional path, `--pages RANGE`, markdown output). OCR is deferred. Hermes's own
`skills/.../scripts/` layout is exactly the folder-skill model co adopts in the prerequisite
plan — co reaches parity on skill structure, not just this one skill.

## Problem & Outcome

**Problem.** co is blind to local PDFs. "Summarize this PDF" is an unconditional "I can't,"
even though the capability is blocked only by a missing dependency + a thin script.

**Failure cost:** the assistant silently cannot serve its single most common local-document
request; the user must extract text by hand outside co and paste it back — defeating the
"personal assistant over your files" purpose. No error teaches them a workaround exists.

**Outcome.**
1. `pymupdf4llm==1.27.2.3` added (version-synced with the engine, per Dev decision
   2026-06-10 — see Dependency layer); pins `pymupdf==1.27.2.3` + `pymupdf-layout==1.27.2.3`
   in lockstep. `pymupdf` declared explicitly (version-unconstrained, defers to the pin)
   because the script imports it directly. The `1.27.x` line makes `pymupdf-layout` a **hard**
   dep, so `onnxruntime`/`numpy`/`networkx`/`protobuf` are pulled (the onnx-backed layout
   model). Still no PyTorch/marker-pdf.
2. `co_cli/skills/documents/scripts/extract_pdf.py` — subprocess CLI with a `main()` entry
   point: PDF → markdown with page-break markers, plain stderr + non-zero exit on errors.
   Output goes through `sys.stdout.write`/`sys.stderr.write` (not `print`) — the script is
   under `co_cli/` and ruff **T20** forbids `print` there (Code Accuracy table). Exposed to the
   agent as the `co-extract-pdf` console entry point (`[project.scripts]`), invoked via
   `shell_exec`.
3. `co_cli/skills/documents/SKILL.md` — locate → extract → answer, PDF-only v1, via `shell_exec`.
4. Real-PDF test + committed fixture; bundled gate green (`documents` in `_BUNDLED_NAMES`).
5. Ships once it passes lint + the bundled gate + the new test (folder-skill model already in code).

## Scope

### In scope
- `pyproject.toml` (+ `uv.lock`) — add `pymupdf` + `pymupdf4llm`; add the
  `co-extract-pdf = "co_cli.skills.documents.scripts.extract_pdf:main"` console entry point to
  `[project.scripts]`.
- `co_cli/skills/documents/__init__.py`, `co_cli/skills/documents/scripts/__init__.py`
  (docstring-only, so the `:main` module path import-resolves).
- `co_cli/skills/documents/scripts/extract_pdf.py` (new) — subprocess CLI with a `main()`
  entry point; output via `sys.stdout.write`/`sys.stderr.write` (T20-clean).
- `co_cli/skills/documents/SKILL.md` (new) — drives `file_search`/`web_fetch`/`file_read`/`shell_exec`.
- `tests/test_flow_skill_documents.py` (new) + a committed fixture PDF.
- `tests/test_flow_skill_bundled_library.py` — register `documents` in `_BUNDLED_NAMES`.

### Out of scope
- **Any new tool, importer, or `categories.py`/`toolset.py` change.** No `document_extract`
  tool; the skill drives `shell_exec` + the helper script — that is the whole design. (openclaw
  is the lone peer that ships a dedicated `pdf` *tool* — but it is in-process clawpdf WASM in a
  TS runtime, no heavy dep, no subprocess concern; that substrate doesn't transfer to a Python
  pymupdf subprocess, so co stays skill+script, matching hermes. Deliberate divergence, not a gap.)
- **The folder-skill model itself** — loader/lifecycle/user-CRUD conversion + migrating the
  existing bundled skills shipped separately in v0.8.332 (`skill-folder-model`), not a task here.
- **Multimodal / native-PDF-to-model input** — handing the raw PDF (or page images) to the
  model's vision / native-PDF capability, the openclaw + opencode pattern (openclaw uses
  Anthropic's `document` block + `pdfs-2024-09-25` beta; opencode attachment pass-through).
  Out of scope here for two reasons: it is the **vision plan**'s territory
  (`2026-05-28-150239-vision-input.md`), and co's target may be a local small model with no
  PDF/vision support — text extraction via pymupdf4llm works regardless of model modality.
- **OCR / scanned-image-only PDFs** — needs tesseract + marker-pdf (~3–5 GB); deferred. co is
  at parity here, not behind: hermes ships an `extract_marker.py` script but does **not** install
  marker by default (manual `pip`), and openclaw/opencode have no real OCR (openclaw falls back
  to render-page-to-image → model vision, which is the multimodal path above, i.e. the vision plan).
- **docx / pptx / xlsx** — owned by the sibling **`office` skill** plan
  (`2026-06-10-114024-skill-office.md`), a separate extraction backend (OOXML / markitdown or
  format-specific libs, no torch). The two skills are reciprocal: this skill routes Office
  formats to `office`; `office` routes `.pdf` back here.
- **Adding `co-extract-pdf` to `DEFAULT_SHELL_SAFE_COMMANDS`** — the user opts in via config
  (`shell.safe_commands += "co-extract-pdf"`); not a shipped default (documented in the body).
- **Spec entry** — `docs/specs/skills.md` is updated by `sync-doc` post-delivery, not a task
  here. The specific tech facts to merge are enumerated under **Spec sync** below.

## Behavioral Constraints
1. **Substrate gate.** Body references only `file_search`, `web_fetch`, `file_read`,
   `shell_exec` (+ the bundled extractor via the `co-extract-pdf` console entry point). No
   `file_find`, no `uv run python -m`.
2. **Marginal-value gate.** Multi-step locate → extract → answer with a decision tree.
3. **Native body.** co tool names only; no `terminal`/`extract_pymupdf.py`/`web_extract`.
4. **Process isolation (hard).** Extraction runs via `shell_exec` subprocess (the
   `co-extract-pdf` console entry point), never imported into the agent process.
5. **Approval transparency.** First extraction prompts approval (`co-extract-pdf` not in
   safe_commands). The opt-in `shell.safe_commands += "co-extract-pdf"` only auto-approves the
   bare command — `shell_policy.py`'s `_FORBIDDEN_ARG_PATTERNS` rejects any arg containing `/`,
   `~`, or `..` (verified `shell_policy.py:11-27`), so a PDF anywhere but the workspace root
   still prompts by design (path-traversal guard, not a bug). The realistic ergonomic win is
   session-subject auto-approval: once approved this session, the same subject is not
   re-prompted (`co_cli/context/orchestrate.py`). The body must describe this accurately and
   not promise the opt-in eliminates the prompt for typical paths.
6. **Graceful failure.** The script exits non-zero with a plain stderr message (no traceback)
   for missing file / non-PDF / corrupt PDF; the **encrypted/password-protected** case gets its
   own distinct message ("PDF is password-protected") so the user learns the recoverable cause,
   per the Failure-cost thesis.
7. **Scanned-PDF detection seam (handoff, not OCR) — pinned contract.** When extraction yields
   empty/near-empty text (an image-only / scanned PDF), the script signals it as **exit code 0
   plus a single sentinel marker line on stdout** — the literal `[no-text-layer: likely scanned]`
   — and **no other stdout**. **Why exit 0, not a non-zero code:** extraction *succeeded* (the
   file opened and parsed fine) — there simply is no text layer; encoding this as a non-zero exit
   would collide with the genuine-error paths (missing / non-PDF / corrupt / password-protected
   all exit non-zero per Constraint 6), and the **tier-2 consumer** (`scanned-pdf-vision`, which
   reuses those same non-zero error codes) must be able to distinguish *"scanned → route to
   `image_view`"* from *"extraction failed → give up."* Exit 0 + a reserved, machine-detectable
   stdout marker is that distinction. This is the seam the tier-2 path (render pages →
   `image_view`) keys off; here it only surfaces the condition so the skill body can route to
   that path or suggest `web_fetch`, **never silently answer from a blank extraction.** The
   emptiness threshold (e.g. a chars-per-page floor) is the script's call; no OCR/rendering
   happens in this plan.
8. **Bounded dependency.** `pymupdf4llm==1.27.2.3` (version-synced) + its lockstep pins
   `pymupdf` + `pymupdf-layout`, which transitively pull `onnxruntime`/`numpy`/`networkx`/
   `protobuf` (the onnx layout model). No PyTorch/marker-pdf. (Revised 2026-06-10: the original
   lean constraint — pymupdf only — was superseded by the Dev decision to track the latest
   version-synced release, which made `pymupdf-layout` a hard dep.)
9. **Read-only synthesis.** No write tools.
10. **Bundled, test-gated.** Lands in `co_cli/skills/documents/SKILL.md`; passes skill-lint
   R1–R4 + B1 + security scan; registered in `_BUNDLED_NAMES`.
11. **Lint-clean script (hard).** `extract_pdf.py` passes `scripts/quality-gate.sh lint`
   (ruff over `co_cli/`). It is **not** in any per-file-ignore, so it must use
   `sys.stdout.write`/`sys.stderr.write` rather than `print` (ruff **T20**), and otherwise
   satisfy the same ruleset as the rest of `co_cli/`.

## High-Level Design

Three layers, bottom-up:

1. **Dependency layer** — `pymupdf4llm==1.27.2.3` (the markdown adapter that emits page chunks) +
   `pymupdf` (the C-backed engine). **Version decision (2026-06-10):** Dev chose to track the
   latest **version-synced** release `1.27.2.3` rather than hold the dep on the pre-`1.x` line.
   The trade-off was surfaced and accepted: the `1.27.x` series made `pymupdf-layout` (and its
   `onnxruntime`/`numpy`/`networkx`/`protobuf` stack — the onnx-backed layout model) a **hard**
   dependency, where the `0.3.x` line kept it an opt-in `[layout]` extra. The earlier "lean,
   pymupdf-only" framing is superseded; the residual hard floor (Constraint 8) is **no PyTorch
   and no marker-pdf**, which still holds. **Why declare `pymupdf` separately, when pymupdf4llm
   pins it anyway?** the script *imports it directly* — `pymupdf.open(path).needs_pass` is how
   Constraint 6's distinct "password-protected" message is produced (pymupdf4llm exposes no
   encryption check), and `doc.page_count` validates `--pages`. Convention: declare what you
   import. **Caveat:** declare `pymupdf` **without a version constraint** so it defers to
   pymupdf4llm's exact `==1.27.2.3` pin — a conflicting co pin would break resolution. The
   bounded-dep check is verifiable: marker-pdf/PyTorch must not appear in the resolved tree.

2. **Extraction layer** — `co_cli/skills/documents/scripts/extract_pdf.py`, a stdlib-`argparse`
   CLI with a `main()` entry point, exposed to the agent as the `co-extract-pdf` console
   command and invoked `co-extract-pdf <path> [--pages RANGE]`. A `[project.scripts]` entry
   point resolves via `PATH` from any `work_dir` and on an installed wheel — unlike `uv run
   python -m` (which resolves the env from the user's `cwd`, where `co_cli` is not installed)
   or a repo-relative file path. See the Context section for the full spawn-env rationale
   (`shell_env.py` allowlist: `PATH` yes, `VIRTUAL_ENV`/`PYTHONPATH` no). `--pages RANGE`
   mirrors hermes's `extract_pymupdf.py` shape (`0-4`, `2`, `0-1,3`); the script **parses it
   into an explicit 0-based list** before calling pymupdf4llm — `to_markdown(pages=…)` takes a
   list of 0-based page numbers, **not** a range string. To guarantee the citation contract
   regardless of library internals, the script calls `to_markdown(path, pages=<list>,
   page_chunks=True)` (which returns per-page chunks) and emits its own explicit `## Page N`
   marker before each page, then writes the concatenated markdown to stdout via
   `sys.stdout.write` (not `print` — ruff T20). On any failure it writes a one-line message to
   stderr and exits non-zero (no traceback). It ships **inside the skill folder** (the
   folder-skill model makes this shippable) and is **never imported into the agent process** —
   the agent reaches it only as a `shell_exec` subprocess, the isolation boundary. `documents/`
   and `documents/scripts/` carry docstring-only `__init__.py` so the `:main` module path
   import-resolves.

3. **Skill layer** — `co_cli/skills/documents/SKILL.md`, a prompt overlay with a Locate → Extract
   → Answer decision tree. Locate uses `file_search` or a user-supplied path; if Google is
   configured, `google_drive_search` is an *optional* additional route (it is DEFERRED +
   config-gated, invisible when Google is unconfigured), so the body degrades to
   `file_search` + user path as the primary, no-Google path. Extract branches (URL →
   `web_fetch`; local `.pdf` → `shell_exec` driving the script; local `.docx`/`.pptx`/`.xlsx`
   → defer to the `office` skill; other local → `file_read`); the local `.pdf` branch calls
   `co-extract-pdf <path>` via `shell_exec`. **Office-format degradation:** the `office` skill
   ships on its own active plan (`2026-06-10-114024-skill-office.md`) and may not be loaded when
   `documents` lands first (see Execution order). The body must degrade honestly for
   `.docx`/`.pptx`/`.xlsx` — name the format and that Office support is the `office` skill's job
   — rather than silently dead-end or hand off to a skill the loader can't find. Answer
   synthesizes with **page N** citations from the script's `## Page N` markers. The skill is
   registered as bundled by adding `documents` to `_BUNDLED_NAMES` in the bundled-library test,
   which is what makes the gate enforce it.

The approval prompt on first extraction is a feature, not friction: it surfaces that co is
about to run Python on the user's file. Repeat use within a session is auto-approved by
subject; the body explains this honestly (see Constraint 5) rather than overselling the
safe_commands opt-in.

## Tasks

### ✓ DONE TASK-1 — Add extraction dependency + console entry point
- **files:** `pyproject.toml`, `uv.lock`
- **prerequisites:** none
- **done_when:** `uv sync` clean; `uv run python -c "import pymupdf4llm, pymupdf"` exits 0; `uv pip list` shows no `torch`/`marker-pdf`; if `pymupdf` is declared explicitly it carries **no version constraint** (defers to pymupdf4llm's exact `==1.27.2.3` pin — no resolution conflict); `[project.scripts]` gains `co-extract-pdf = "co_cli.skills.documents.scripts.extract_pdf:main"` and after `uv sync` the `co-extract-pdf` command is on `PATH` (`uv run which co-extract-pdf` exits 0). Note: `uv sync` writes the entry-point wrapper **without importing its target**, so it succeeds even before `extract_pdf.py` exists — but `co-extract-pdf` only *runs* once TASK-2 lands `extract_pdf.py:main`. Land TASK-1's `pyproject` edit and TASK-2 together so the command is actually runnable (the end-to-end run is asserted in TASK-2/TASK-4, not here).
- **success_signal:** N/A (dependency + entry-point wiring).

### ✓ DONE TASK-2 — Helper script `documents/scripts/extract_pdf.py` (with `main()` entry point)
- **files:** `co_cli/skills/documents/__init__.py`, `co_cli/skills/documents/scripts/__init__.py` (docstring-only), `co_cli/skills/documents/scripts/extract_pdf.py`
- **prerequisites:** TASK-1 (`pyproject` entry point + deps; folder-skill model already in code — `documents/` is a skill folder).
- **done_when:** the script exposes a `main()` function (the `[project.scripts]` target) and emits all output via `sys.stdout.write`/`sys.stderr.write` (no `print` — passes ruff T20); `co-extract-pdf <fixture>.pdf` writes markdown with an explicit `## Page N` marker before each page (synthesized by the script from `page_chunks=True`); `--pages 0-1` parses to the list `[0, 1]` and prints only those two pages; a missing path, a non-PDF extension, and a password-protected PDF each exit non-zero with a distinct one-line stderr message and no traceback; an **image-only/scanned PDF (empty text layer)** signals per Constraint 7's pinned contract — **exit 0** with the single sentinel line `[no-text-layer: likely scanned]` as the only stdout (never a non-zero exit, never blank stdout); `scripts/quality-gate.sh lint` is clean.
- **success_signal:** Running `co-extract-pdf` against a real PDF returns its text as markdown with page markers on stdout.

### ✓ DONE TASK-3 — `documents` skill (bundled + registered)
- **files:** `co_cli/skills/documents/SKILL.md`, `tests/test_flow_skill_bundled_library.py`
- **prerequisites:** TASK-2
- **done_when:** `documents` added to `_BUNDLED_NAMES`; `uv run pytest tests/test_flow_skill_bundled_library.py` passes (loads + security-scan clean); `lint_bundled_extras` returns zero **B1** findings and the `loader.py` security scan is empty (these are the *enforced* gates — R1–R4 are advisory and never block); body references only existing co tools (no `file_find`) and the `co-extract-pdf` console command (no `uv run python -m`), and is < 8000 chars; the skill appears in `/skills list` after `/skills reload`.
- **trigger-quality (the description is the model's only selection signal — `index.py:20` puts `{name, description}` in the manifest, nothing else):** the frontmatter `description` names the user intents that should fire it (read / summarize / ask about a **local PDF**) and the format scope it owns (PDF) vs defers (Office → the `office` skill), in plain task language, not implementation terms. Validated behaviorally by a dispatch check: a representative "summarize this PDF / what does this PDF say" prompt selects `documents`, and a `.docx`/URL prompt does **not** select it (Office is the sibling skill's job; a bare web URL is `web_fetch`). A description that under-fires (skill never chosen → "I can't read PDFs" fallback) or over-fires (grabbed for non-PDF requests) fails this gate even with a perfect body.
- **success_signal:** Asking co to summarize a local PDF triggers the `documents` skill, which extracts via the module and answers with page citations.

### ✓ DONE TASK-4 — Functional extraction test + fixtures
- **files:** `tests/test_flow_skill_documents.py` (new), `tests/skills/fixtures/<sample>.pdf` (new text-PDF fixture) and a scanned/image-only fixture `tests/skills/fixtures/<scanned>.pdf` (the `tests/skills/fixtures/` dir does not exist yet and is created by this task)
- **prerequisites:** TASK-2
- **done_when:** `uv run pytest tests/test_flow_skill_documents.py` passes. The test invokes the **real shipped invocation** — `subprocess.run(["co-extract-pdf", ...])` (the console entry point installed by `uv sync` in TASK-1; this also functionally proves the entry-point wiring) — against committed fixtures with **real `pymupdf4llm`, no mocks**. Functional assertions, mirroring TASK-2's `done_when`:
  - happy path: text-PDF fixture → stdout contains the fixture's known text and a `## Page N` marker; exit 0;
  - `--pages 0-1` → only those two pages present in stdout; exit 0;
  - missing path, non-PDF extension, password-protected PDF → each exits non-zero with its distinct one-line stderr message (assert the message text, not just the code);
  - scanned/image-only fixture → **exit 0** and stdout is exactly the sentinel line `[no-text-layer: likely scanned]` (Constraint 7's pinned contract); assert both the zero exit and the marker text, and that stdout is **not** silently blank and **not** a non-zero error exit.
  All assertions are behavioral (observable stdout/stderr/exit), never structural.
- **success_signal:** N/A (test authoring).

## Testing
One new test file (`tests/test_flow_skill_documents.py`) **functionally validates the PDF
extraction by running the real `co-extract-pdf` console command via `subprocess.run`** against
committed fixtures — real `pymupdf4llm`, no mocks — covering the happy path, `--pages`,
the three error paths (missing / non-PDF / password-protected), and the scanned-PDF signal
(see TASK-4). This is the primary functional gate for the script; skill loading/lint is
covered by the bundled-library gate once `documents` is in `_BUNDLED_NAMES`. Run scoped:
`uv run pytest tests/test_flow_skill_documents.py tests/test_flow_skill_bundled_library.py 2>&1 | tee .pytest-logs/$(date +%Y%m%d-%H%M%S)-documents.log`.
Manual: `/skills reload`, `/skills lint documents`, `skill_view documents`, and an
end-to-end extract on a real PDF (observe the first-run approval prompt).

## Open Questions
1. **Q:** `--pages RANGE` vs `--max-pages N`? **A (resolved):** `--pages RANGE` — matches
   hermes's `extract_pymupdf.py`. Note `pymupdf4llm.to_markdown(pages=…)` takes a **0-based
   list, not a range string**, so the script parses `RANGE` → list, and uses `page_chunks=True`
   to synthesize the `## Page N` markers the citation contract needs (plain `to_markdown()`
   string output does not reliably emit per-page boundaries).
2. **Q:** Where do the fixture PDFs live? **A (resolved):** `tests/skills/fixtures/` — this
   subdir does **not** exist yet (`tests/skills/` has only `__init__.py` + a test file), so
   TASK-4 creates it; a tiny 1–2 page text PDF **plus** a tiny image-only/scanned PDF (to
   exercise Constraint 7), both committed. (Kept in the test tree, not the skill folder — they
   are test data, not shipped skill assets.)
3. **Q:** marker-pdf for scanned PDFs? **A (rec):** Deferred — ~3–5 GB. The peer-aligned path
   for scanned/image-only PDFs is render-page-to-image → model vision (openclaw's fallback),
   which belongs to the **vision plan** (`2026-05-28-150239-vision-input.md`), not here. This
   skill's body notes the limitation and, when the source is a URL, suggests `web_fetch` as the
   no-vision fallback.
4. **Q:** Drive remote case? **A (rec):** Phase-1 Locate may use `google_drive_search`;
   extraction is local. A Drive-synced file already on disk is in scope; raw remote stays
   with `google_drive_read` / `web_fetch`.
5. **Q (resolved):** How does the agent invoke the script portably? **A:** a `[project.scripts]`
   console entry point `co-extract-pdf`, not `uv run python -m` (which can't resolve `co_cli`
   under `shell_exec`'s allowlist env / user-`cwd`) — see Context and High-Level Design layer 2.
## Spec sync — merge into `docs/specs/skills.md` on delivery

`sync-doc` runs post-delivery (see Out of scope). These are the **specific runtime facts**
this plan establishes that must land in `docs/specs/skills.md` so the bundled-skill-with-assets
model is documented canon, not tribal knowledge:

1. **Skill folders may bundle an executable asset** invoked as a shell subprocess — the
   `documents` skill is the first to do so (`co_cli/skills/documents/scripts/extract_pdf.py`).
2. **Bundled skill scripts are exposed via a `[project.scripts]` console entry point**
   (`co-extract-pdf`), never `uv run python -m`. Rationale is a runtime constraint, not a
   preference: `shell_exec` spawns with a **sanitized allowlist env** (`shell_env.py` — `PATH`
   propagates, `VIRTUAL_ENV`/`PYTHONPATH` do not) and `cwd` = the user's `workspace_dir`, so
   only a `PATH`-resolved console command resolves `co_cli` reliably on dev checkout *and*
   installed wheel. This is the **convention for any future skill-bundled script.**
3. **Skill-bundled scripts live under `co_cli/` and obey the full `co_cli/` ruff ruleset**
   (notably T20 — output via `sys.stdout.write`/`sys.stderr.write`, not `print`), because
   per-file-ignores do not cover `co_cli/skills/*/scripts/`.
4. **Approval semantics for skill-driven shell commands** — first run prompts; the
   `shell.safe_commands` opt-in plus the `_FORBIDDEN_ARG_PATTERNS` path-traversal guard means
   only workspace-root bare paths auto-approve; session-subject auto-approval is the real
   ergonomic win (Constraint 5).

`sync-doc` should fold facts 1–4 into the skills spec's bundled-asset section (creating it if
absent); the `documents` skill itself is listed in the bundled-skill catalog as usual.

## Final — Team Lead

Revised: helper script relocated from unshipped repo-root `scripts/` into the skill folder
(`co_cli/skills/documents/scripts/extract_pdf.py`). The folder-skill model this relies on is
now **shipped (v0.8.332)**, so this plan is unblocked.

**Gate-1 revision (2026-06-10, source-verified):** two findings folded in.
- (A) **Lint blocker** — the script lives under `co_cli/` where ruff **T20** forbids `print`
  and no per-file-ignore covers `co_cli/skills/*/scripts/`; output must use
  `sys.stdout.write`/`sys.stderr.write` (Constraint 11, Code Accuracy table).
- (B) **Invocation** — `uv run python -m …` cannot resolve `co_cli` under `shell_exec`'s
  allowlist spawn env (`shell_env.py` propagates `PATH`, not `VIRTUAL_ENV`/`PYTHONPATH`; `cwd`
  is the user's `workspace_dir`), so the script is exposed as a `[project.scripts]` console
  entry point `co-extract-pdf` (parallels `co` itself) and invoked by bare command. Threads
  through TASK-1/2/3/4, Constraint 5, High-Level Design layer 2, and the Spec-sync section.

The corresponding runtime facts are marked for the skills spec under **Spec sync** above.
Prior approval was void on the revision — re-review at Gate 1.

> Gate 1 — PO review required before proceeding.
> Review this plan: right problem? correct scope?
> Once approved, run: `/orchestrate-dev skill-documents`.

## Delivery Summary — 2026-06-10

| Task | done_when | Status |
|------|-----------|--------|
| TASK-1 | `uv sync` clean; imports exit 0; no torch/marker-pdf; `pymupdf` unconstrained; `co-extract-pdf` on PATH | ✓ pass |
| TASK-2 | `main()` entry point; `sys.stdout/stderr.write` (T20); `## Page N` markers; `--pages` range→list; 4 error paths exit non-zero w/ distinct one-line stderr; scanned→exit 0 + sole sentinel; lint clean | ✓ pass |
| TASK-3 | `documents` in `_BUNDLED_NAMES`; bundled gate passes; B1 + security scan empty; body uses only co tools + `co-extract-pdf`; <8000 chars; in model manifest | ✓ pass |
| TASK-4 | real `subprocess.run(["co-extract-pdf", …])`, real pymupdf4llm, committed fixtures; happy/`--pages`/3 error paths/scanned all behaviorally asserted | ✓ pass |

**Tests:** scoped — 11 passed, 0 failed (`tests/test_flow_skill_documents.py` 10 + `tests/test_flow_skill_bundled_library.py` 1). The documents suite covers every distinct script exit path: multi-page extraction, clean-stdout regression guard (fd-1 silencing), `--pages` range/single, and all five error branches (missing / non-PDF / corrupt / invalid-pages / password-protected) + the scanned sentinel.
**Doc Sync:** fixed — `docs/specs/skills.md` gained a "Bundled Executable Assets" subsection (Spec-sync facts 1–4) and a refined File Format note; `documents` named as the first bundled skill with an executable asset.

**Dependency-version decision (2026-06-10, user-directed):** the plan's original *lean* premise (Constraint 8: pymupdf only) was superseded. The newest `pymupdf4llm` (`1.27.2.3`) makes `pymupdf-layout` a **hard** dependency (the `0.3.x` line kept it an opt-in `[layout]` extra). Surfaced the trade-off; the user chose to **version-sync at `1.27.2.3`**, accepting the `onnxruntime`/`numpy`/`networkx`/`protobuf` stack (the onnx layout model) in exchange for tracking the latest engine-locked release. Plan amended: Outcome #1, Constraint 8 (now "Bounded dependency"), and the Dependency layer record this. Residual hard floor — no PyTorch, no marker-pdf — still holds and is verified in the resolved tree.

**Files:** `pyproject.toml` (+`uv.lock`), `co_cli/skills/documents/{__init__.py, SKILL.md, scripts/__init__.py, scripts/extract_pdf.py}`, `tests/test_flow_skill_bundled_library.py`, `tests/test_flow_skill_documents.py`, `tests/skills/fixtures/{text,scanned,protected}.pdf`.

**Out-of-scope changes present in the tree (NOT this plan — do not stage at ship):** `docs/exec-plans/active/2026-05-28-150239-vision-input.md`, `docs/exec-plans/active/2026-06-10-144745-scanned-pdf-vision.md` (sibling plans, pre-modified at session start), and untracked `docs/exec-plans/active/2026-06-10-221629-merge-case-into-run.md`.

**Residual verification (for /review-impl):** the TASK-3 trigger-quality gate has a behavioral sub-check — a "summarize this PDF" prompt must select `documents` while a `.docx`/URL prompt must not. The structural/lint/manifest gates are green; the live LLM-dispatch confirmation is best done as a manual/eval check (real model call), not asserted in pytest.

**Overall: DELIVERED**
All four tasks pass `done_when`; lint clean; scoped tests green (8/8); doc sync applied. One user-directed scope change (dependency version-sync) folded into the plan.

## Implementation Review — 2026-06-10

### Evidence
| Task | done_when | Spec Fidelity | Key Evidence |
|------|-----------|---------------|-------------|
| TASK-1 | deps + entry point; no torch/marker; pymupdf unconstrained; command on PATH | ✓ pass | `pyproject.toml:27,28,33`; `uv pip list` no torch/marker-pdf; layout stack present (intended); `which co-extract-pdf` → `.venv/bin/co-extract-pdf` |
| TASK-2 | `main()`; T20-clean output; `## Page N` markers; `--pages`→list; 4 error paths distinct non-zero; scanned→exit 0 + sole sentinel; lint clean | ✓ pass | `extract_pdf.py:135` main; writes at 52/160/168 (no print); scanned via raw `get_text()` at `_resolve_pages:95` (placeholder-proof); fd-1 silenced+restored in `_extract:110-120` finally (no leak); live exit=1 on missing/non-PDF/corrupt/password |
| TASK-3 | in `_BUNDLED_NAMES`; bundled gate passes; B1+scan empty; co-tool-only body; <8000; in manifest | ✓ pass | `test_flow_skill_bundled_library.py:17`; scan `[]`, B1 `[]`, body_len 3401, in_manifest True; no `file_find`/`uv run python -m`; `user-invocable:false` (model-invocable capability skill) |
| TASK-4 | real `subprocess.run(["co-extract-pdf",…])`, real pymupdf4llm, committed fixtures, behavioral-only | ✓ pass | `test_flow_skill_documents.py:27-31`; fixtures are real PDF binaries (`file`→PDF v1.7); zero mocks/patch; every assert observes exit/stdout/stderr |

### Issues Found & Fixed
No blocking issues found. Evidence subagents (two cold adversarial reads) returned PASS on every `done_when` clause; adversarial re-check of the three highest-risk items (fd-1 redirect leak-safety, scanned-detection robustness, exit-code propagation) confirmed correct.

One non-blocking observation (not a defect, not changed): `_render`'s `chunk.metadata.page` lookup (`extract_pdf.py:128`) is always-None in pymupdf4llm 1.27.2.3, so the positional fallback always runs — verified correct by order preservation (`--pages 2,0,0` → Page 1 + Page 3). Defensible inline forward-compat guard, not a speculative helper.

### Tests
- Command: `uv run pytest -x -q`
- Result: 663 passed, 0 failed (147s; no slow/stalled calls — slowest 0.01s)
- Scoped documents+bundled: 11 passed
- Log: `.pytest-logs/*-review-impl.log`

### Behavioral Verification
- `co-extract-pdf tests/skills/fixtures/text.pdf` → page-marked markdown (`## Page 1`/`## Page 2` + text), exit 0 — **TASK-2 success_signal verified**.
- `/skills list` in live `co chat` REPL → `documents` present with its description (loaded via the real bootstrap path) — **TASK-3 skill registration verified live**.
- TASK-3 success_signal (model *selects* the skill on a "summarize this PDF" prompt): skill confirmed in the live model-facing manifest; the live LLM-dispatch is the one piece warranting a manual smoke — description is well-scoped (no over/under-firing risk found). Recommended as a pre/post-ship manual check, non-blocking.
- (`co status` is not a command in this CLI; used the actual `co chat` REPL surface instead.)

### Scope note
Out-of-scope tree changes NOT part of this plan and NOT to be staged at ship: `evals/*.py` + `evals/_observability.py` (coworker's in-progress eval `trace_files` cleanup, unrelated to documents — verified no `documents`/`pdf`/`extract` references), and pre-modified sibling plans `2026-05-28-150239-vision-input.md`, `2026-06-10-144745-scanned-pdf-vision.md` (+ untracked `2026-06-10-221629-merge-case-into-run.md`).

### Overall: PASS
All four tasks meet their `done_when` contracts; full suite green (663 passed); lint clean; behavioral verification confirms the command output and live skill registration. Ready for Gate 2 → `/ship`.
