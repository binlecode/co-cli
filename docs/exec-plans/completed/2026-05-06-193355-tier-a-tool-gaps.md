# Plan: Tier A Tool Gaps — memory-doc sync

Task type: doc

## Context

This plan closes the documentation-only Tier A gap identified in
`docs/reference/RESEARCH-tools-peers-tiers.md`: phantom `memory_list` /
`memory_read` references in `CLAUDE.md` and the peer-survey reference
doc.

> **Split note (2026-05-07).** The original plan bundled this doc-sync
> work with a hermes-parity skill tools port. The skill tools track has
> moved to its own plan
> `docs/exec-plans/active/2026-05-07-125538-skill-tools-hermes-port.md`
> for focus and delivery quality (signature parity, return-shape parity,
> three deferred parity items, expanded test coverage). The third
> originally-listed Tier A item (`web_fetch` multi-URL) was dropped at
> C1 review — pydantic-ai's agent loop already runs concurrent tool
> calls when the model emits multiple `web_fetch(url=…)` invocations in
> one assistant turn (`is_concurrent_safe=True` at
> `co_cli/tools/web/fetch.py:105`); see the Deferred items section.

The doc-sync item kept here:

1. **Doc sync (Track A — no code)** — `CLAUDE.md:43-47` and
   `docs/reference/RESEARCH-tools-peers-tiers.md` (~lines 440-441,
   727-728) claim `memory_list` and `memory_read` exist as registered
   tools. They do not. `memory_read_session_turn` is defined in
   `co_cli/tools/memory/read.py:50` but intentionally unregistered per
   `docs/specs/memory.md:114`. This is a pure documentation fix with
   zero code surface — it was already blocking downstream gap analyses
   (this very planning cycle started from a phantom-tool reading).

### Current-state validation (inline)

- ✓ `CLAUDE.md:45` lists `memory_list` as a registered tool — phantom claim.
- ✓ `RESEARCH-tools-peers-tiers.md:440-441` lists "Paginated memory inventory
  (`memory_list`)" and "Full memory artifact read by slug (`memory_read`)" as
  co-cli unique — both phantom.
- ✓ `docs/specs/memory.md:114` correctly documents that
  `memory_read_session_turn` exists in source but is intentionally not
  registered. This is the source of truth — other docs realign here.
- ✓ No active plan exists for slug `tier-a-tool-gaps`.
- ✓ No stale TODO blocking — recent active plans cover unrelated work.

## Problem & Outcome

**Problem:** Onboarding docs claim phantom tools. Phantom
`memory_list` / `memory_read` rows in `CLAUDE.md` and the peer-survey
reference doc actively misled this planning cycle. Future review and
audit cycles will hit the same trap until fixed.

**Outcome:** `CLAUDE.md` and `RESEARCH-tools-peers-tiers.md` accurately
describe the registered memory-tool surface (3 tools: `memory_search`,
`memory_create`, `memory_modify`).

## Scope

### In scope
- Doc edits to `CLAUDE.md:43-50` and
  `docs/reference/RESEARCH-tools-peers-tiers.md` memory-related rows.

### Out of scope
- `web_fetch` multi-URL form — deferred to a future Tier B item, contingent
  on a real research-turn trace showing the model does *not* already
  parallel-dispatch `web_fetch(url=…)` calls. See Deferred items.
- Implementing `memory_list` or `memory_read` as new tools — this plan
  agrees with the architecture decision in `docs/specs/memory.md:114`
  (search subsumes browse via empty/kind-filtered queries).
- Registering `memory_read_session_turn` — not the gap being closed.
- `skills_list` / `skill_view` model-callable tools — moved to
  `2026-05-07-125538-skill-tools-hermes-port.md`.

## Behavioral Constraints

1. **Doc sync is information-conserving.** No claims removed unless
   verified against `co_cli/agent/_native_toolset.py:NATIVE_TOOLS` or
   `docs/specs/memory.md`. The reference doc keeps its structure;
   only the affected rows change.

## High-Level Design

- `CLAUDE.md:43-50`: replace the 4-tool block. Lead with the
  architecture decision so contributors searching for "list" find it:

  > Recall is search-driven: there is no `memory_list` or
  > `memory_read` tool. Browsing is `memory_search` with an empty or
  > kind-filtered query; full-body artifact reads use the generic
  > `file_read` tool against the path that `memory_search` surfaces.
  >
  > Three unified `memory_*` tools cover the active surface:
  > - `memory_search` — recall across artifacts (BM25) + sessions
  >   (LLM-summarized) + canon in one call
  > - `memory_create` — save a new artifact (all kinds: user | rule |
  >   article | note)
  > - `memory_modify` — append or surgically replace a passage in an
  >   existing artifact
  >
  > Canon hits ship full body inline; sessions ship LLM summaries inline.
  > A `memory_read_session_turn` reader exists in source but is
  > intentionally not registered — see `docs/specs/memory.md` for the
  > rationale.

- `docs/reference/RESEARCH-tools-peers-tiers.md`:
  - §2.5 Memory and Recall: change the co-cli column for "Paginated memory
    inventory" and "Full memory artifact read by slug" from `✓ unique`
    to `✗ deliberate gap` with a note pointing to `docs/specs/memory.md`.
  - §3 Tier 3 / T3-M co-cli unique extensions table: remove the two
    phantom rows. Keep all other rows.
  - Appendix Tool Name Mapping: change the co-cli cell for `memory_list`
    and `memory_read` from a tool name to `—`.
  - Re-verify the §3 "Tier 1 coverage: 10 / 11 applicable" statistic —
    `memory_list`/`memory_read` were not in the T1 roster, so the count
    likely needs no change. Confirm during the edit.

## Tasks

### TASK-1 — Sync CLAUDE.md memory tool block (Track A) ✓ DONE

- **files:**
  - `CLAUDE.md`
- **prerequisites:** —
- **done_when:**
  `grep -E "memory_list|memory_read[^_]" CLAUDE.md | wc -l` equals 0
  (no phantom references), AND `grep -c "memory_search\|memory_create\|memory_modify" CLAUDE.md`
  is at least 3.
- **success_signal:** Onboarding contributors reading CLAUDE.md see
  accurate tool count; downstream gap analyses build on truth.

Replace the 4-tool block with the architecture-first wording from
§High-Level Design / Doc sync above.

### TASK-2 — Sync RESEARCH-tools-peers-tiers.md memory rows (Track A) ✓ DONE

- **files:**
  - `docs/reference/RESEARCH-tools-peers-tiers.md`
- **prerequisites:** —
- **done_when:**
  `grep -nE "memory_list|memory_read[^_]" docs/reference/RESEARCH-tools-peers-tiers.md`
  returns no rows that *claim the tools exist as registered*.
  Permitted remaining matches: explicit "deliberate gap" / "not
  implemented" annotations and the cross-reference Appendix row marked `—`.
- **success_signal:** Future peer-survey reviews no longer build on
  phantom rows.

Edits:
- §2.5 Memory and Recall rows: change co-cli ✓ entries for "Paginated
  memory inventory" and "Full memory artifact read by slug" to
  `✗ deliberate gap` with footnote pointing to `docs/specs/memory.md`.
- §3 T3-M co-cli unique extensions table: remove the two phantom rows.
- Appendix Tool Name Mapping: change `memory_list` and `memory_read`
  cells for co-cli to `—`.
- Verify the §3 Tier 1 coverage statistic and adjust if it is affected
  (likely no change — `memory_list`/`memory_read` were T2/T3, not T1).

Find target rows by searching for the strings `memory_list` and
`memory_read` rather than line numbers — line numbers drift as the file
evolves.

## Testing

### Lint / quality gate
- `scripts/quality-gate.sh lint` after the doc edits land.

## Deferred items

- **`web_fetch` multi-URL form** — deferred contingent on a real
  research-turn trace. Decision criteria: a Tier B task is justified
  only if traces show the model does *not* already parallel-dispatch
  `web_fetch(url=…)` calls in a single response. If they show
  parallel dispatch already happens, the remaining benefit is
  token-envelope reduction (~100 tokens × N URLs); evaluate whether
  that justifies the schema/error-handling complexity (CD-M-2/3/4
  raised in C1 review). Tracker: open in Tier B backlog when revisited.
- **`skills_list` / `skill_view` model-callable tools** — moved to its
  own plan: `2026-05-07-125538-skill-tools-hermes-port.md`. Includes
  three further deferred parity items (skills-as-directories loader,
  plugin namespace, view_count telemetry).

## Shipping order

Single commit — TASK-1 + TASK-2. Both are doc-only, lint-gated. Already
✓ DONE; this plan is essentially shippable as-is.

## Final — Team Lead

Plan approved.

> Gate 1 — PO review required before proceeding.
> Once approved, run: `/orchestrate-dev tier-a-tool-gaps`
